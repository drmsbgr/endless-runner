namespace RatRush.Enums
{
    public enum Lane
    {
        Left,
        Middle,
        Right
    }

    public enum GameStatus
    {
        Paused,
        Running,
        Over
    }

    public enum CatStatus
    {
        Far,
        Near,
        Nearest
    }
    public enum ObstacleType
    {
        Normal,
        Bench,
        Trashcan,
        Human,
    }
}